{
  "buildCommand": "bundle install && bundle exec jekyll build && mkdir -p _site/admin && cp admin/config.yml _site/admin/config.yml && cp admin/index.html _site/admin/index.html",
  "outputDirectory": "_site"
}